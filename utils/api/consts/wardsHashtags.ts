import base from '@/utils/api/base';

const controller = 'api/WardsHashtags';

const CONSTS_WARDS_HASHTAGS = {
    listarHashtagQtd: `${base}/${controller}/listarHashtagQtd`
};

export default CONSTS_WARDS_HASHTAGS;