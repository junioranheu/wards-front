import Styles from './index.module.scss';

export default function Final() {
    return (
        <section className={Styles.final}>
            <span>Veja mais posts xxx</span>
        </section>
    )
}